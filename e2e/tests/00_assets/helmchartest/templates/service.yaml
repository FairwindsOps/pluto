apiVersion: v1
kind: Service
metadata:
  name: {{ include "helmchartest.fullname" . }}
  labels:
    {{- include "helmchartest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helmchartest.selectorLabels" . | nindent 4 }}
