apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helmchartest.fullname" . }}-test-connection"
  labels:
    {{- include "helmchartest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helmchartest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
